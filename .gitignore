node_modules/
npm-debug.log*

# Android build outputs / local machine config (regenerated by `npx cap sync android`)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/

# Release signing (never commit keystores or credentials)
*.keystore
*.jks
android/keystore.properties
